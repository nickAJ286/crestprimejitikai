<%= @user.building.name %> <%= @user.room %>号室<%= @user.family_name %><%= @user.first_name %>
<%# <%= @user.role.name %>
<%= form_with( model: @user,  local: true) do |f| %>
  <%= f.collection_select(:role_id, Role.all, :id, :name, {}) %>
  <%= f.submit "更新する"  %>
      <%=link_to 'トップページ', root_path %>
<% end %>
<%# url: users_path(@user), %>